# test file for ca-certificates
---
- hosts: all
  remote_user: vagrant
  become: true
  pre_tasks:
    - name: include vars
      include_vars: main.yml
    - import_tasks: pre.yml
  roles:
    - ../../
